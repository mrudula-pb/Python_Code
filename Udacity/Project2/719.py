# Given an integer array, return the k-th smallest distance
# among all the pairs. The distance of a pair (A, B) is defined
# as the absolute difference between A and B.

class Solution:
    def smallestDistancePair(self, nums: List[int], k: int) -> int:
        return nums